[project]
name = "d2-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joseph", email = "underradicals@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
d2-data = "d2_data:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
